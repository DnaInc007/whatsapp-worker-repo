services:
  whatsapp-worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fdispatch-whatsapp-worker
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
    command: npm run whatsapp:worker
